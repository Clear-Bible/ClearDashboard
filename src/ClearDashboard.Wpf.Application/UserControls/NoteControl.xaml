<UserControl x:Class="ClearDashboard.Wpf.Application.UserControls.NoteControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClearDashboard.Wpf.Application.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FontSize="16">
    <UserControl.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>
    <Grid>
     <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
     </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Notes for" HorizontalContentAlignment="Center" />
        <Label Grid.Row="1" 
               Content="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=TokenDisplay.SurfaceText}" 
               HorizontalContentAlignment="Center"
               FontWeight="Bold"
               FontSize="20"/>
        <TextBox Grid.Row="2" x:Name="TranslationValue" Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=TokenDisplay.Note}" Margin="10,0"></TextBox>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="10" Click="ApplyNote">Apply</Button>
            <Button Margin="10" Click="Cancel">Cancel</Button>
        </StackPanel>
    </Grid>
</UserControl>
