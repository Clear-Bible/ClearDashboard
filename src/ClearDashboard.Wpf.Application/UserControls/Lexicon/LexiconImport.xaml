<UserControl x:Class="ClearDashboard.Wpf.Application.UserControls.Lexicon.LexiconImport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:cm="http://caliburnmicro.com"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:helpers="clr-namespace:ClearDashboard.Wpf.Application.Helpers;assembly=ClearDashboard.Wpf.Application.Abstractions"
             xmlns:resx="clr-namespace:ClearDashboard.Wpf.Application.Strings;assembly=ClearDashboard.Wpf.Application.Abstractions"
             xmlns:lexicon="clr-namespace:ClearDashboard.Wpf.Application.ViewModels.Lexicon"
             xmlns:converters="clr-namespace:ClearApplicationFoundation.Framework.Converters;assembly=ClearApplicationFoundation"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             helpers:Translation.ResourceManager="{x:Static resx:Resources.ResourceManager}"
             Background="{DynamicResource MaterialDesignPaper}"
             Foreground="{DynamicResource MaterialDesignBody}"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=lexicon:LexiconViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
       
        <!--<system:Boolean x:Key="True">True</system:Boolean>
        <system:Boolean x:Key="False">False</system:Boolean>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />-->
      
        <Style TargetType="{x:Type Label}">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource MahApps.Brushes.SystemControlForegroundAccent}" />
        </Style>
        <Style x:Key="RowButtonStyle" TargetType="{x:Type Button}" >
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="MinHeight" Value="50" />
            <Setter Property="Width" Value="170"></Setter>
            <Setter Property="Margin" Value="5"></Setter>
            <Setter Property="Padding" Value="5"></Setter>
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border>
                            <Border.Style>
                                <Style TargetType="{x:Type Border}">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="LightGray" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="False">
                                            <Setter Property="Background" Value="#EEEBEB" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Background" Value="Transparent" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Background="Transparent">
                                <ContentPresenter x:Name="ButtonContent" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="1" />
                    <Setter Property="Foreground" Value="Gray" />
                    <Setter Property="Background" Value="LightGray" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
        <TabControl HorizontalContentAlignment="Left" Margin="5">
            <TabItem Header="Import">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" ></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Button x:Name="ProcessLexiconToImport"
                                cm:Message.Attach="ProcessLexiconToImport"
                                Width="100"
                                Height="30"
                                Margin="10, 0"
                                Foreground="{DynamicResource MaterialDesignBody}"
                                HorizontalAlignment="Left"
                                Content="{helpers:Localization LexiconImport_ImportChecked}" 
                                Style="{StaticResource SmallTransparentStyle}"
                                IsEnabled="{Binding HasLexiconToImport}"/>
                        <StackPanel Orientation="Horizontal" Margin="10,5,5,5">
                            <Label Margin="10, 0, 0, 0" Content="Corpus:"></Label>
                            <ComboBox
                                x:Name="ParatextProjectsComboBox"
                                MinWidth="150"
                                Margin="10,0,5,0"
                                IsEditable="False"
                                ItemsSource="{Binding ProjectCorpora}"
                                SelectedItem="{Binding SelectedProjectCorpus, Mode=TwoWay}"
                                cm:Message.Attach="ProjectCorpusSelected">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="16" Text="{Binding Name}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                    <materialDesign:Card Grid.Row="1" Grid.Column="0" Visibility="{Binding HasSelectedProjectCorpus, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <StackPanel>
                        <TextBlock FontSize="18" Margin="10" Text="{helpers:Localization LexiconImport_PleaseChooseACorpus}" ></TextBlock>
                        </StackPanel>
                    </materialDesign:Card>
                <materialDesign:Card Grid.Row="1" Grid.Column="0" Visibility="{Binding ShowNoRecordsToManageMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                        <TextBlock FontSize="18" Margin="10" Text="{helpers:Localization LexiconImport_NoMoreRecordsToImport}" ></TextBlock>
                        </StackPanel>
                    </materialDesign:Card>
                <DataGrid Grid.Row="1" Grid.Column="0" 
                          x:Name="LexiconSelectionDataGrid" 
                          ItemsSource="{Binding LexiconToImport}"
                          AutoGenerateColumns="False" 
                          HorizontalAlignment="Stretch"
                          Margin="5"
                          VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          VirtualizingPanel.IsVirtualizing="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.VerticalScrollBarVisibility="Visible"
                          IsReadOnly="True"
                          Visibility="{Binding HasLexiconToImport, Converter={StaticResource BooleanToVisibilityConverter}}">

                    <DataGrid.Columns>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <CheckBox x:Name="SelectAllCheckBox" 
                                            cm:Message.Attach="[Event Checked] = [Action OnToggleAllChecked($source)];[Event Unchecked] = [Action OnToggleAllChecked($source)]" 
                                            cm:Action.TargetWithoutContext="{Binding DataContext, ElementName=LexiconSelectionDataGrid}">
                                            <CheckBox.Resources>
                                                <ToolTip x:Key="CheckedToolTip">
                                                    <TextBlock Text="{helpers:Localization LexiconImport_SelectAll}"></TextBlock>
                                                </ToolTip>
                                                <ToolTip x:Key="UncheckedToolTip">
                                                    <TextBlock Text="{helpers:Localization LexiconImport_UnselectAll}"></TextBlock>
                                                </ToolTip>
                                            </CheckBox.Resources>
                                            <CheckBox.Style>
                                                <Style TargetType="CheckBox">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=SelectAllCheckBox, Path=IsChecked}" Value="False">
                                                            <Setter Property="CheckBox.ToolTip"
                                                    Value="{StaticResource CheckedToolTip}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ElementName=SelectAllCheckBox, Path=IsChecked}" Value="True">
                                                            <Setter Property="CheckBox.ToolTip"
                                                   Value="{StaticResource UncheckedToolTip}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </CheckBox.Style>
                                        </CheckBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox Focusable="False" IsChecked="{Binding Path=IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_SourceWord}"  Binding="{Binding SourceWord}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_SourceLanguage}" Binding="{Binding SourceLanguage}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_SourceType}" Binding="{Binding SourceType}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_TargetWord}"  Binding="{Binding TargetWord}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_TargetLanguage}" Binding="{Binding TargetLanguage}" Width="150"/>
                        <DataGridTemplateColumn  Width="250" Header="{helpers:Localization LexiconImport_LexemeOrFormMatches}" SortMemberPath="ShowAddAsFormButton" CanUserSort="True">
                               <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button  cm:Message.Attach="[Event Click] = [Action OnAddAsFormButtonClicked($dataContext)]" 
                                                 cm:Action.TargetWithoutContext="{Binding DataContext, ElementName=LexiconSelectionDataGrid}"
                                                Style="{StaticResource RowButtonStyle}" 
                                                ToolTip="{helpers:Localization LexiconImport_AddFormButtonTooltip}"
                                                 Visibility="{Binding ShowAddAsFormButton, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Button.ContentTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{helpers:Localization LexiconImport_AddFormButtonText}" TextWrapping="Wrap"></TextBlock>
                                                </DataTemplate>
                                            </Button.ContentTemplate>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        <DataGridTemplateColumn  Width="250" Header="{helpers:Localization LexiconImport_TranslationMatches}" SortMemberPath="ShowAddTargetAsTranslationButton" CanUserSort="True">
                              <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button  cm:Message.Attach="[Event Click] = [Action OnTargetAsTranslationButtonClicked($dataContext)]" 
                                                 cm:Action.TargetWithoutContext="{Binding DataContext, ElementName=LexiconSelectionDataGrid}"
                                                Style="{StaticResource RowButtonStyle}" 
                                                ToolTip="{helpers:Localization LexiconImport_AddTargetButtonTooltip}"
                                                Visibility="{Binding ShowAddTargetAsTranslationButton, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Button.ContentTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{helpers:Localization LexiconImport_AddTargetButtonText}" TextWrapping="Wrap" ></TextBlock>
                                                </DataTemplate>
                                            </Button.ContentTemplate>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem
                Header="Imported">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" ></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <DataGrid Grid.Row="1" Grid.Column="0" 
                        x:Name="ImportedLexiconSelectionDataGrid" 
                        ItemsSource="{Binding ImportedLexicon}"
                        AutoGenerateColumns="False" 
                        HorizontalAlignment="Stretch"
                        Margin="4"
                        VirtualizingPanel.IsVirtualizingWhenGrouping="True"
                        VirtualizingPanel.VirtualizationMode="Recycling"
                        VirtualizingPanel.IsVirtualizing="True"
                        SelectionMode="Single"
                        SelectionUnit="FullRow"
                        ScrollViewer.CanContentScroll="True"
                        ScrollViewer.VerticalScrollBarVisibility="Visible"
                        IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_SourceWord}"  Binding="{Binding SourceWord}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_SourceLanguage}" Binding="{Binding SourceLanguage}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_SourceType}" Binding="{Binding SourceType}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_TargetWord}"  Binding="{Binding TargetWord}" Width="150"/>
                            <DataGridTextColumn Header="{helpers:Localization LexiconImport_TargetLanguage}" Binding="{Binding TargetLanguage}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

    <!--</materialDesign:Card>-->

</UserControl>
