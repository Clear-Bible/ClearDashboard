<Window
    x:Class="ClearDashboard.Wpf.Application.Views.PopUps.AccountInfoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:ClearDashboard.Wpf.Application.Helpers;assembly=ClearDashboard.Wpf.Application.Abstractions"
    xmlns:local="clr-namespace:ClearDashboard.Wpf.Application.Views.PopUps"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:ClearDashboard.Wpf.Application.Strings;assembly=ClearDashboard.Wpf.Application.Abstractions"
    Title="AboutView"
    Width="600"
    MaxHeight="450"
    helpers:Translation.ResourceManager="{x:Static resx:Resources.ResourceManager}"
    FontFamily="{StaticResource ClearDashboardFont}"
    mc:Ignorable="d">
    <Border BorderBrush="Black" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label
                Grid.Row="0"
                Margin="0,10,0,0"
                HorizontalAlignment="Left"
                Content="-CURRENT ACCOUNT DETAILS-"
                FontWeight="Bold" />

            <StackPanel
                Grid.Row="1"
                Margin="10"
                HorizontalAlignment="Left"
                Orientation="Vertical">
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="ClearDashboard Username:" />
                    <Run Text="{Binding ClearDashboardUsername}" />
                </TextBlock>
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="ClearDashboard UserId:" />
                    <Run Text="{Binding ClearDashboardId}" />
                </TextBlock>

                <TextBlock
                    Margin="0,10,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="Collaboration Username:" />
                    <Run Text="{Binding CollaborationUsername}" />
                </TextBlock>
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="Collaboration Email:" />
                    <Run Text="{Binding CollaborationEmail}" />
                </TextBlock>
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="Collaboration UserId:" />
                    <Run Text="{Binding CollaborationId}" />
                </TextBlock>

                <TextBlock
                    Margin="0,10,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="Paratext Username:" />
                    <Run Text="{Binding ParatextUsername}" />
                </TextBlock>
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Run FontWeight="Bold" Text="Paratext Email:" />
                    <Run Text="{Binding ParatextEmail}" />
                </TextBlock>
            </StackPanel>

            <Button
                x:Name="DeactivateCurrentLicense"
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="1"
                Width="170"
                Height="40"
                Margin="3,10,3,5"
                Content="Deactivate Current License"
                IsEnabled="{Binding IsEnabled}"
                Style="{StaticResource TransparentStyle}" />

            <StackPanel
                Grid.Row="2"
                Grid.ColumnSpan="2"
                Margin="10"
                HorizontalAlignment="Left"
                Orientation="Vertical"
                Visibility="{Binding UserAndPathListVisibility}">
                <Label
                    HorizontalAlignment="Left"
                    Content="-OTHER ACCOUNTS-"
                    FontWeight="Bold" />

                <Label HorizontalAlignment="Left" Content="Select an Account to Switch to:" />

                <ListBox
                    IsEnabled="{Binding IsEnabled}"
                    ItemsSource="{Binding UserAndPathList}"
                    SelectedItem="{Binding SelectedUserAndPath}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Row="0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center">
                                    <Run FontWeight="Bold" Text="ClearDashboard Username:" />
                                    <Run Text="{Binding Item1.FullName, Mode=OneWay}" />
                                </TextBlock>
                                <TextBlock
                                    Grid.Row="1"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center">
                                    <Run FontWeight="Bold" Text="ClearDashboard UserId:" />
                                    <Run Text="{Binding Item1.Id}" />
                                </TextBlock>
                                <TextBlock
                                    Grid.Row="2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center">
                                    <Run FontWeight="Bold" Text="Collaboration Email:" />
                                    <Run Text="{Binding Item3.RemoteEmail}" />
                                </TextBlock>
                            </Grid>

                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

            </StackPanel>



            <Button
                x:Name="Close"
                Grid.Row="3"
                Grid.Column="1"
                Width="170"
                Height="40"
                Margin="10"
                Content="{helpers:Localization Close}"
                IsEnabled="{Binding IsEnabled}"
                Style="{StaticResource AlternativeStyle}"
                Visibility="{Binding CloseWindowVisibility}" />

            <Button
                x:Name="RestartApplication"
                Grid.Row="3"
                Grid.Column="1"
                Width="170"
                Height="40"
                Margin="10"
                Content="Restart Application"
                IsEnabled="{Binding IsEnabled}"
                Style="{StaticResource AlternativeStyle}"
                Visibility="{Binding CloseApplicationVisibility}" />
        </Grid>
    </Border>
</Window>
