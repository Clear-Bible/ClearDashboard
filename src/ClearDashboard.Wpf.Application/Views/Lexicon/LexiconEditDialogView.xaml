<UserControl x:Class="ClearDashboard.Wpf.Application.Views.Lexicon.LexiconEditDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             d:DataContext="{d:DesignInstance Type=lexicon:LexiconEditDialogViewModel}"
             xmlns:helpers="clr-namespace:ClearDashboard.Wpf.Application.Helpers;assembly=ClearDashboard.Wpf.Application.Abstractions"
             xmlns:resx="clr-namespace:ClearDashboard.Wpf.Application.Strings;assembly=ClearDashboard.Wpf.Application.Abstractions"
             xmlns:lexicon="clr-namespace:ClearDashboard.Wpf.Application.ViewModels.Lexicon"
             helpers:Translation.ResourceManager="{x:Static resx:Resources.ResourceManager}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid MinWidth="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="40" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="25" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="60" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
      
        <Grid Margin="5" >
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition Height="35" />
                <RowDefinition Height="35" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="150"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBlock  Grid.Row="0"  Grid.Column="0"
                        Text="{helpers:Localization LexiconEdit_SourceLanguage}"
                        HorizontalAlignment="Center"></TextBlock>
            <ComboBox x:Name="SourceLanguage" 
                      Grid.Row="0" 
                      Grid.ColumnSpan="2" 
                      Grid.Column="1" 
                      Margin="5"
                      ItemsSource="{Binding SourceLanguages}"
                      SelectedItem="{Binding SelectedSourceLanguage}"
                      IsEnabled="{Binding EnableSourceLanguageComboBox}"></ComboBox>

            <TextBlock  Grid.Row="1"  Grid.Column="0"  
                        Text="{helpers:Localization LexiconEdit_TargetLanguage}"
                        HorizontalAlignment="Center"></TextBlock>
            <ComboBox x:Name="TargetLanguage" 
                      Grid.Row="1" 
                      Grid.ColumnSpan="2" 
                      Grid.Column="1" 
                      Margin="5"
                      ItemsSource="{Binding TargetLanguages}"
                      SelectedItem="{Binding SelectedTargetLanguage}"
                      IsEnabled="{Binding EnableTargetLanguageComboBox}"></ComboBox>

            <CheckBox Grid.Row="2" Grid.Column="0"></CheckBox>
        </Grid>
      
      

        <StackPanel Grid.Row="6"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <Button x:Name="Save" 
                    Content="{helpers:Localization LexiconEdit_SaveChanges}"
                    FontFamily="{DynamicResource MaterialDesignFont}"
                    FontSize="14"
                    Margin="10"
                    IsEnabled="{Binding CanSave}"
                    Style="{StaticResource TransparentStyle}"
            />
            <Button x:Name="Cancel" 
                    Content="{helpers:Localization LexiconEdit_Cancel}"
                    FontFamily="{DynamicResource MaterialDesignFont}"
                    FontSize="14"
                    Margin="10"
                    IsEnabled="{Binding CanCancel}"
                    Style="{StaticResource TransparentStyle}"
            />
        </StackPanel>
        <ProgressBar Grid.Row="2"
                     Grid.RowSpan="3"
                     Width="76"
                     Height="75"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Foreground="{DynamicResource SecondaryHueMidBrush}"
                     IsIndeterminate="True"
                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                     Visibility="{Binding ProgressBarVisibility}"
                     Value="0" />
    </Grid>
</UserControl>
